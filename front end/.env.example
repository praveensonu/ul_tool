# Backend origin only; the frontend API client appends /api.
VITE_API_URL=http://localhost:8000

# Used only when VITE_API_URL is empty and Vite proxies same-origin /api calls.
API_PROXY_TARGET=http://127.0.0.1:8000
